# VOXELLANEOUS

## BUILD

### WGPU RENDERER BACKEND (RUST -> WASM MODULE)

in voxellaneous-core:

```
wasm-pack build --target web
```

### FRONTEND (TYPESCRIPT)

in voxellaneous-web:

```
npm install
npm run dev
```
