# TODO

## Сетевые улучшения
- Дельта‑снапшоты: отправлять только изменения, а не полный state.
- Interest management: сервер шлёт только сущности в радиусе интереса игрока.
- Добавить `sequence` для снапшотов и отбрасывать устаревшие пакеты.
- Синхронизация времени (ping/pong offset) для стабильной интерполяции.

## Качество симуляции
- Input sequence + server acknowledgement + client replay (классический prediction/reconciliation).
- Ограничения скорости и базовая валидация на сервере (anti‑cheat).

## Транспорт и надёжность
- Разделить каналы: unreliable для позиций, reliable для важных событий (spawn/despawn/чат).
- Дополнительное сжатие бинарных пакетов при росте числа сущностей.

